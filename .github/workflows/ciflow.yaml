name: CI on Every Push

on: push

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Check out the repository code
      - name: Checkout code
        run: echo "helllo world"
        

  upload-pkg:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Check out the repository code
      - name: Checkout code
        run: echo "helllo world-2"        
